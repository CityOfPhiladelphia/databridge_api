CARTO_TOKEN='some_token'
# Exact location for Databridge-Public / PostgREST server
POSTGREST_DB_URL='https://url' 
# Exact location of `/convert` endpoint to translate SQL to PostgREST
POSTGREST_SQL_URL='https://url'
# The root path added by the proxy
ROOT_PATH='/databridge-api/v1' 

# Only populate these for running pytests
AGO_USER='some_user'
AGO_PASSWORD='some_password'
AGO_URL='https://arcgis.com'